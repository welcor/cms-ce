#
# The main properties file.
#

# License key for vertical site


# -- Admin settings --
# Default values:
# cms.admin.password = password
cms.admin.email = test@enonic.com
# cms.admin.defaultLanguage = en

# -- Mail settings --
# Default values:
cms.mail.smtpHost = 127.0.0.1

# -- Scheduler settings --
# Default values:
cms.scheduler.enabled = false
# cms.scheduler.tx.timeout = 3600

# -- Cache settings --
# Note: timeToLive is in seconds.
# Default values:
# cms.cache.entity.memoryCapacity = 500000
# cms.cache.entity.diskCapacity = 0
# cms.cache.entity.timeToLive = 0
# cms.cache.blob.memoryCapacity = 200
# cms.cache.blob.diskCapacity = 2000
# cms.cache.blob.timeToLive = 0
# cms.cache.page.memoryCapacity = 10000
# cms.cache.page.diskCapacity = 0
# cms.cache.page.timeToLive = 86400
# cms.cache.image.memoryCapacity = 1000
# cms.cache.image.diskCapacity = 10000
# cms.cache.image.timeToLive = 0
# cms.cache.localization.memoryCapacity = 100
# cms.cache.localization.diskCapacity = 0
# cms.cache.localization.timeToLive = 600

# -- Cluster settings --
# Default values:
# cms.cluster.enabled = false
# cms.cluster.jgroups.name = default
# cms.cluster.jgroups.multicast = 231.12.21.132

# Manually configure dialect name. Valid names: auto, db2, derby, oracle, mysql, sqlserver, postgres
#cms.jdbc.dialect = auto
#cms.jdbc.logging = false
#cms.jdbc.connectionTrace = true

# -- Tool settings --
cms.tools.allowHosts = *

# UrlRewrite settings
cms.urlrewrite.logging = false

cms.userstore.connector.versiontest.userPolicy = all
cms.userstore.connector.versiontest.groupPolicy = read 
cms.userstore.connector.versiontest.plugin = ldap
cms.userstore.connector.versiontest.plugin.dialect = generic
cms.userstore.connector.versiontest.plugin.url = ldap://versiontest:389
cms.userstore.connector.versiontest.plugin.authDn = cn=root,dc=com
cms.userstore.connector.versiontest.plugin.authPassword = vertical
cms.userstore.connector.versiontest.plugin.userBaseDn = dc=enonic,dc=com
cms.userstore.connector.versiontest.plugin.groupBaseDn = dc=enonic,dc=com

cms.userstore.connector.hugetest.userPolicy = all
cms.userstore.connector.hugetest.groupPolicy = all 
cms.userstore.connector.hugetest.plugin = ldap
cms.userstore.connector.hugetest.plugin.dialect = generic
cms.userstore.connector.hugetest.plugin.url = ldap://versiontest:389
cms.userstore.connector.hugetest.plugin.authDn = cn=root,dc=com
cms.userstore.connector.hugetest.plugin.authPassword = vertical
cms.userstore.connector.hugetest.plugin.userBaseDn = dc=users,dc=hugetest,dc=com
cms.userstore.connector.hugetest.plugin.groupBaseDn = dc=groups,dc=hugetest,dc=com

cms.userstore.connector.bender.userPolicy = all
cms.userstore.connector.bender.groupPolicy = all 
cms.userstore.connector.bender.plugin = ldap 
cms.userstore.connector.bender.plugin.dialect = ad
cms.userstore.connector.bender.plugin.url = ldap://bender.enonic:389
cms.userstore.connector.bender.plugin.authDn = cn=Administrator,cn=users,dc=enonic,dc=local
cms.userstore.connector.bender.plugin.authPassword = ATAnonic88
cms.userstore.connector.bender.plugin.userBaseDn = ou=nssrtest,dc=enonic,dc=local
cms.userstore.connector.bender.plugin.groupBaseDn = ou=nssrtest,dc=enonic,dc=local



