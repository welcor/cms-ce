
apply plugin: 'war'

war {
    into("META-INF/maven/${project.group}/${project.name}") {
        from('src/main/misc/pom.properties')
        expand("project": project)
    }
}

dependencies {
	compile project(':cms-core')
	providedCompile "javax.servlet:servlet-api:${versions.servlet}"
}

